apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "my_app.fullname" . }}
  labels:
    {{- include "my_app.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["endpoints"]
    verbs: ["list", "get"]
